 <section id="gigs" class="container">
    <h1>All Blog Posts</h1>

    {{#each trails}}
    <div class="gig">
      <h3>{{nayme}}</h3>
      <p>{{location}}</p>
      <ul>
        <li>{{length}}</li>
      </ul>
      <p>{{blog}}</p>
    </div>
    {{else}}
    <p>no blogs were found</p>
    {{/each}}


  </section>
